This piece runs quietly in the background as the bridge between the app and the file-storage system used for things like photos and documents. It has two different modes: a normal mode used by the main app, which lets a person add and retrieve their own files, and a "Pātaka mode" used only by the backup-keeping server, which just keeps a full copy of everyone's files without ever adding or serving any of its own.
